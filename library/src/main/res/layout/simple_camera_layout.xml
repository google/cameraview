<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <include
        layout="@layout/include_simple_camera"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/take_picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="@dimen/activity_vertical_margin"
        android:adjustViewBounds="true"
        android:onClick="onCameraButtonClicked"
        app:srcCompat="@drawable/ic_camera"/>

    <ImageButton
        android:id="@+id/back"
        style="@style/TransparentCircleBackButton"
        android:layout_width="@dimen/transparent_back_button_size"
        android:layout_height="@dimen/transparent_back_button_size"
        android:layout_gravity="left|top"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="10dp"
        android:onClick="onBackClicked"/>

</FrameLayout>
